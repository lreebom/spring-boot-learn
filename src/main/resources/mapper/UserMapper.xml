<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lreebom.springbootlearn.mapper.UserMapper">
    <select id="selectById" resultType="com.lreebom.springbootlearn.model.entity.User">
        select id, username, email, status, deleted, create_time, update_time
        from user
        where id = #{id}
          and deleted = 0
    </select>

    <select id="selectByUsername" resultType="com.lreebom.springbootlearn.model.entity.User">
        select id, username, email, status, deleted, create_time, update_time
        from user
        where username = #{username}
          and deleted = 0
    </select>

    <select id="selectByEmail" resultType="com.lreebom.springbootlearn.model.entity.User">
        select id, username, email, status, deleted, create_time, update_time
        from user
        where email = #{email}
          and deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user (username, email)
        values (#{username}, #{email})
    </insert>

    <select id="selectPage" resultType="com.lreebom.springbootlearn.model.entity.User">
        select id, username, email, status, deleted, create_time, update_time
        from user
        where deleted = 0
        order by create_time desc
        limit #{offset}, #{limit}
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(*)
        from user
        where deleted = 0
    </select>

    <update id="update" parameterType="com.lreebom.springbootlearn.model.entity.User">
        update user
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id}
        and deleted = 0
    </update>

    <delete id="deleteById">
        update user
        set deleted = 1
        where id = #{id}
          and deleted = 0
    </delete>
</mapper>